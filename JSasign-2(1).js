const clothes=['jacket', 't-shirt'];
clothes.length=0;
console.log(clothes[0]);

//JavaScript executes clothes.length = 0, all the items of the array clothes are deleted. clothes[0] is undefined, because clothes array was emptied

